# Standup — 2026-04-20

**Run:** xsantcastx-backlog-executor-v2 (scheduled)
**Branch:** main (origin pushed to `d983a45`)
**Pre-flight:** dirty working tree from prior crashed run held 27 in-progress embed routes, the PortfolioService SSR guard, and a broken char-map.component.ts. Reset would have lost work, so verified each diff against the open Notion tickets and shipped them as atomic commits.

## Shipped (4 atomic commits, all pushed to origin/main)

1. **`4f893d4`** `fix(char-map): replace ASCII apostrophes with \u2018/\u2019/\u201C/\u201D escapes`
   - Pre-existing build blocker — lines 501-504 in `src/app/tools/char-map/char-map.component.ts` used `'''/'''/'\"'/'\"'` for the smart-quote characters, which the TS parser reads as empty string + stray apostrophe (40+ TS1005 errors).
   - Replaced with explicit unicode escapes. `tsc -p tsconfig.app.json` now exits 0.

2. **`f434572`** `fix(ssr): add 27 missing /embed/* routes for tools added to prerender-routes.txt`
   - Closes ROOT CAUSE 1 of [Notion 348e6899-...c75229](https://www.notion.so/348e6899f10e81c3afcfe533c8c75229).
   - Added embed routes for: ascii-art, button-generator, checklist, color-name, color-picker, countdown, gitignore-generator, heading-checker, hmac-generator, html-minifier, js-minifier, json-diff, json-tree, jwt-cheatsheet, license-picker, line-sorter, mime-lookup, og-image-preview, package-json, regex-cheatsheet, scroll-snap, spacing-scale, timezone-converter, transform-playground, transition-generator, ua-parser, webhook-tester.
   - All 27 component imports already existed in app-routing.module.ts; only the `{ path: 'embed/<slug>', component: <Comp>, data: { embed: true } }` declarations were missing.

3. **`327fd0c`** `fix(ssr): short-circuit PortfolioService HTTP calls on the server`
   - Inject PLATFORM_ID, return `of([])` when `!isPlatformBrowser`. Both datasets are progressive-enhancement so visible behavior is unchanged. Stops one HTTP fetch path that was failing the prerender worker against `http://ng-localhost/...`.

4. **`d983a45`** `seo(og): add og-color-palette.png — fix broken social preview for /tools/color-palette`
   - Closes [Notion 348e6899-...abcf1](https://www.notion.so/348e6899f10e8139aa59c149227abcf1) (Agent + High).
   - Generated 1200x800 PNG using brand palette (#00ffcc / #7b61ff / #ff00ff / #1a1a2e). Card includes wordmark, sample dominant-color swatches with hex labels, and outline chips for export formats.

## Not shipped / residual blockers

- **SSR Root Cause 2 (`XC.setProperty` NotYetImplemented)** — still open. Detailed static-analysis audit written to `outputs/ssr-xc-setproperty-audit-2026-04-20.md`:
  - All explicit source `setProperty(--xxx)` calls are isBrowser-guarded.
  - All `[style.--xxx]` template bindings already migrated by 54c724f.
  - Conclusion: error originates in `@angular/ssr` or `@angular/platform-server` hydration runtime, not user code. The `[attr.style]` migration may have just moved the failure rather than eliminating it.
  - Recommended single change that addresses both this ticket AND the High SSRF CVE GHSA-45q2-gjvg-7973: upgrade `@angular/platform-server` to latest 20.3.x patch. Deferred — needs a clean test-build pass that exceeds this run's time budget.

- **Changelog not posted live** — `scripts/add-changelog-entry.js` again hit `EAI_AGAIN firestore.googleapis.com`. Both entries saved at `outputs/changelog-entry-2026-04-20.sh` for manual run.

## Notion writes this run

- `348e6899-...c75229` (Fix SSR Prerender Failures): moved to **In Progress**, notes updated with Root Cause 1 closed + Root Cause 2 audit summary + recommended next step.
- `348e6899-...abcf1` (SEO: og-color-palette.png): moved to **Done**, notes record commit hash and design choices.

## Env quirks worked around

- `.git/index.lock`, `.git/HEAD.lock`, `.git/refs/heads/main.lock`, and `.git/objects/*/tmp_obj_*` are persistently un-removable in this sandbox — `rm` returns "Operation not permitted" but `mv` to `.git/garbage/` succeeds. Same workaround as the 2026-04-19 run. Used `git write-tree` + `git commit-tree` + `git update-ref` plumbing to bypass the standard commit's lock-file dependency.
- `tsc --noEmit -p tsconfig.json` reports 17 pre-existing `TS4111` errors in `functions/` (Cloud Functions backend, separate tsconfig with stricter `noPropertyAccessFromIndexSignature`). Out of scope for this run but worth a future cleanup.

## Tomorrow's priority (suggested)

1. Either upgrade `@angular/platform-server` to close SSR Root Cause 2 + High CVE in one shot, or implement the class-based theming fallback for the 8 components that previously had `[style.--xxx]`.
2. Run `bash outputs/changelog-entry-2026-04-20.sh` from a network-enabled environment to post the changelog.
3. Pick up next-highest Agent Backlog item — the next two High items by recency are `Resolve Pending TODOs: AdSense Config + Stripe Key Verification` and `CSS: Standardize Media Query Breakpoints Across All Components`.
